# Intentionally keep BRICK_HOST empty so the entrypoint uses the container hostname.
# Set to a resolvable hostname if you really need to override; do NOT set this to the raw container IP.
BRICK_HOST=

# Hostname Gluster should use (container_name/hostname). Keep in sync with compose.
HOSTNAME_GLUSTER=gluster-solo

# Port window for Gluster data connections (fits in home NATs & firewalls)
DATA_PORT_START=49152
MAX_PORT=49251
