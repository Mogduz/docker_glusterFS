# === [DOC] Autogenerierte Inline-Dokumentation: volumes.yml.example ===
# Datei: examples/volume/volumes.yml.example
# Typ: YAML (Compose/Volumes/Beispiele).
# Zweck: Deklarative Konfiguration (Container-Stack, Volume-Spezifikationen).
# Hinweis: Kommentare erläutern Felder, sinnvolle Defaults und Sicherheitsüberlegungen.
# === [DOC-END] ===

# Gluster volumes specification (read by entrypoint.sh)
volumes:
  - name: gv0
    replica: 2
    transport: tcp
    options:
      auth.allow: 10.0.1.0/24,192.168.1.0/24
      nfs.disable: on
      performance.client-io-threads: on
      cluster.quorum-type: auto
    # Optional: einzelne Optionen zurücksetzen (CSV)
    options_reset: performance.io-thread-count,auth.allow
    quota:
      limit: 200G
      soft_limit_pct: 80      # akzeptiert 80 oder '80%' (wirkt via 'quota default-soft-limit')

  - name: gv1
    replica: 2
    transport: tcp
    options:
      performance.quick-read: on
      performance.read-ahead: on
    quota:
      limit: 1T
